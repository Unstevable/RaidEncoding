# RaidEncoding
A simple GitHub repo to store old C files pertaining to Raid encoding.
Raid.c performs the RAID Encoding with a given .txt file, and Diar.c performs the decoding process back to its original state.
